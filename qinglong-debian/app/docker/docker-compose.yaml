version: '3.8'
services:
  qinglong-debian:
    image: whyour/qinglong:debian
    container_name: qinglong-debian
    restart: unless-stopped
    ports:
      - "${TRIM_SERVICE_PORT}:5700"
    volumes:
      # 动态数据目录路径（var 文件夹）
      - "${TRIM_PKGVAR}/data:/ql/data"